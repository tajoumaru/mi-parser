name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18, 20, 22]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install CLI dependencies for build
        run: npm install commander yaml

      - name: Build project
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Test library import (no CLI deps)
        run: |
          # Test that the core library works without CLI dependencies
          npm uninstall commander yaml
          node -e "
            import('./dist/mediainfo.js').then(m => {
              const result = m.parseMediaInfo('General\nFormat: MP4\n');
              console.log('✓ Library import test passed');
              console.log('Format:', result.general.format);
            }).catch(e => {
              console.error('✗ Library import test failed:', e.message);
              process.exit(1);
            });
          "

  lint:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install CLI dependencies
        run: npm install commander yaml

      - name: Check TypeScript compilation
        run: npx tsc --noEmit 